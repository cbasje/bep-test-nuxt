import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.thegreenvillage.heatcitymap',
  appName: 'Heat City Map',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
